# Elastic Beanstalk — Node.js 24 on 64bit Amazon Linux 2023
# Environment variables should be set in EB Console → Configuration → Software → Environment properties

option_settings:
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production
